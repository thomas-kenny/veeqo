<%= render 'shared/item_card' %>
<%= link_to "Finish order",

            order_path(@order),
            method: :patch,
            data: { confirm: "Are you sure?" } %>






<%= render "shared/boxcard" %>

<div id="threed-test"></div>

<button type="button" class="btn btn-primary mt-4" data-toggle="modal" data-target="#exampleModal">
  View this box
</button>

<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-sm" role="document">
    <div class="modal-content">
      <div class="modal-body p-0">
        <button type="button" class="close position-absolute text-white" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= render '3d_view' %>
      </div>
    </div>
  </div>
</div>

